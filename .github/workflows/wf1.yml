name: my first worflow concurrency
on: 
  workflow_dispatch: 
jobs:
  job1:
    name: job1
    runs-on: windows-latest
    steps:
      - name: step 1 - running single command in Job1 
        run : echo "Hi from job1 runner"
      - name : step 2 - check os 
        run : dir 
  job2:
    name : job2 
    runs-on: windows-latest
    steps:
      - name : step1:echo name
        run : echo "rachid"
      - name : step2:echo last name
        run : echo "errami"
  
